# Jekyll Garden 網站更新與修改指南

## 目錄
1. [快速開始](#快速開始)
2. [日常更新內容](#日常更新內容)
3. [修改 UI 設計](#修改-ui-設計)
4. [修改頁面架構](#修改頁面架構)
5. [進階客製化](#進階客製化)
6. [部署流程](#部署流程)
7. [常見問題](#常見問題)

---

## 快速開始

### 專案結構
```
jekyll-garden/
├── _config.yml          # 網站主要設定檔
├── _layouts/            # 頁面版型
│   └── Post.html       # 主要版型檔案
├── _includes/           # 可重複使用的組件
│   ├── Nav.html        # 導航欄
│   ├── Content.html    # 內容區塊
│   ├── Footer.html     # 頁尾
│   ├── Feed.html       # 文章列表
│   ├── Search.html     # 搜尋功能
│   └── Backlinks.html  # 反向連結
├── assets/              # 靜態資源
│   ├── css/            # 樣式檔案
│   │   └── style.css   # 主要 CSS 檔案
│   ├── js/             # JavaScript 檔案
│   └── img/            # 圖片資源
├── pages/               # 靜態頁面
│   ├── index.md        # 首頁
│   ├── about.md        # 關於頁面
│   ├── now.md          # Now 頁面
│   └── writing.md      # 寫作頁面
├── _notes/              # 筆記文章
│   └── Public/         # 公開的筆記
└── _posts/              # 部落格文章
```

---

## 日常更新內容

### 1. 修改首頁內容

編輯 `pages/index.md`：

```markdown
---
layout: Post
permalink: /
title: 你的標題
---

# 你的首頁內容

這裡可以寫任何 Markdown 格式的內容。

支援 **粗體**、*斜體*、[連結](https://example.com) 等。
```

### 2. 新增文章

在 `_notes/Public/` 目錄下創建新的 `.md` 檔案：

```markdown
---
title: "文章標題"
date: 2025-02-04
---

文章內容...

可以使用 [[其他文章標題]] 來建立 Wiki 風格的內部連結。
```

### 3. 修改導航選單

編輯 `_config.yml` 的 `menu` 區段：

```yaml
menu:
  - title: "About"
    url: "/about"
  - title: "Now"
    url: "/now"
  - title: "Writing"
    url: "/writing"
  - title: "新增項目"    # 新增選單項目
    url: "/new-page"
```

### 4. 推送更新到網站

```bash
# 1. 查看變更
git status

# 2. 加入變更
git add .

# 3. 提交變更
git commit -m "更新網站內容"

# 4. 推送到 GitHub
git push origin main

# 5. 等待 2-3 分鐘，GitHub Actions 會自動部署
# 查看部署狀態: https://github.com/yhchangcc/yhchangcc.github.io/actions
```

---

## 修改 UI 設計

### 1. 顏色配置

編輯 `assets/css/style.css` 的 `:root` 區段：

```css
:root {
  /* 淺色主題 */
  --bg: rgb(255, 252, 240);      /* 背景色 */
  --bg2: rgb(242, 240, 229);     /* 次要背景色 */
  --text: #6f6e69;               /* 文字色 */
  --title: #343331;              /* 標題色 */
  --brand: rgb(58, 169, 159);    /* 品牌色/強調色 */
  --border: rgb(230, 228, 217);  /* 邊框色 */
}

/* 深色主題 */
[data-theme="dark"] {
  --bg: #18181c;
  --bg2: #23232a;
  --text: #d6d6d6;
  --title: #fff;
  --brand: #3ac9b0;
  --border: #33343a;
}
```

#### 顏色修改範例

想要改成藍色調的網站：

```css
:root {
  --bg: #f0f4f8;              /* 淺藍灰背景 */
  --bg2: #e1e8ed;             /* 深一點的藍灰 */
  --text: #5a6c7d;            /* 藍灰文字 */
  --title: #1a2332;           /* 深藍標題 */
  --brand: #0066cc;           /* 藍色強調 */
  --border: #cbd5e0;          /* 藍灰邊框 */
}
```

### 2. 字體設定

修改 `assets/css/style.css`：

```css
:root {
  /* 修改字體 */
  --font: "Inter", sans-serif;  /* 更換為其他字體 */
  --weight-medium: 400;
  --weight-bold: 600;

  /* 字體大小比例 */
  --scale-xs: 0.8rem;     /* 12px - 小字 */
  --scale-sm: 0.933rem;   /* 14px - 次要文字 */
  --scale-base: 1rem;     /* 15px - 正文 */
  --scale-lg: 1.2rem;     /* 18px - 小標題 */
  --scale-xl: 1.4rem;     /* 21px - 大標題 */
}
```

#### 使用中文字體範例

在 `_layouts/Post.html` 的 `<head>` 中加入：

```html
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@400;700&display=swap" rel="stylesheet">
```

然後修改 CSS：

```css
:root {
  --font: "Noto Sans TC", "Inter", sans-serif;
}
```

### 3. 間距與佈局

```css
:root {
  /* 間距系統 */
  --space-xs: 0.5rem;   /* 8px */
  --space-sm: 1rem;     /* 16px */
  --space-md: 1.5rem;   /* 24px */
  --space-lg: 2rem;     /* 32px */
  --space-xl: 3rem;     /* 48px */

  /* 內容寬度 */
  --max-width: 48rem;   /* 768px - 可以改成 60rem 讓內容更寬 */

  /* 行高 */
  --line-height: 1.5;   /* 可以改成 1.6 或 1.7 */
}
```

### 4. 添加自定義樣式

在 `assets/css/style.css` 底部添加：

```css
/* 自定義樣式 */

/* 修改連結樣式 */
a {
  color: var(--brand);
  text-decoration: underline;
  transition: opacity 0.2s;
}

a:hover {
  opacity: 0.7;
}

/* 修改程式碼區塊 */
pre {
  background: var(--bg2);
  border-radius: 8px;
  padding: var(--space-md);
}

/* 添加陰影效果 */
.note-preview {
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
}

/* 圓角按鈕 */
button {
  border-radius: 20px;
}
```

---

## 修改頁面架構

### 1. 修改導航欄

編輯 `_includes/Nav.html`：

```html
<nav>
  <div class="nav-container">
    <!-- Logo 或網站名稱 -->
    <a href="/" class="site-title">
      {{ site.heading }}
    </a>

    <!-- 導航選單 -->
    <ul class="nav-menu">
      {% for item in site.menu %}
      <li>
        <a href="{{ item.url }}">{{ item.title }}</a>
      </li>
      {% endfor %}
    </ul>

    <!-- 新增：主題切換按鈕 -->
    <button id="theme-toggle">🌙</button>
  </div>
</nav>
```

### 2. 修改頁尾

編輯 `_includes/Footer.html`：

```html
<footer>
  <div class="footer-container">
    <!-- 版權資訊 -->
    <p>{{ site.footer.copyright }}</p>

    <!-- 新增：社群媒體連結 */
    <div class="social-links">
      <a href="https://twitter.com/yourhandle">Twitter</a>
      <a href="https://github.com/yourusername">GitHub</a>
      <a href="mailto:your@email.com">Email</a>
    </div>

    <!-- 原有的 Credits 連結 -->
    {% if site.footer.credits.enabled %}
    <a href="{{ site.footer.credits.url }}">
      {{ site.footer.credits.text }}
    </a>
    {% endif %}
  </div>
</footer>
```

### 3. 創建新的頁面版型

例如創建一個全寬版型 `_layouts/Wide.html`：

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <!-- 複製 Post.html 的 head 內容 -->
</head>
<body>
    <div class="container-wide">  <!-- 改為 wide 容器 -->
        {% include Nav.html %}

        <main class="wide-content">
            <h1>{{ page.title }}</h1>
            {{ content }}
        </main>

        {% include Footer.html %}
    </div>
</body>
</html>
```

然後在 CSS 中定義：

```css
.container-wide {
  max-width: 1200px;  /* 更寬的容器 */
  margin: 0 auto;
}

.wide-content {
  display: grid;
  grid-template-columns: 1fr 1fr;  /* 兩欄佈局 */
  gap: var(--space-lg);
}
```

使用新版型：

```markdown
---
layout: Wide
title: "寬版頁面"
---

內容...
```

### 4. 修改首頁佈局

編輯 `_layouts/Post.html` 中的首頁區段（第 58-69 行）：

```html
<!-- Homepage Layout-->
{% if page.permalink == "/" %}
  {% if site.preferences.homepage.enabled %}
    <!-- 自定義首頁佈局 -->
    <div class="homepage">
      <div class="hero-section">
        {% include Content.html %}
      </div>

      <!-- 新增：特色文章區 -->
      <div class="featured-posts">
        <h2>精選文章</h2>
        <!-- 這裡可以加入特定文章 -->
      </div>

      <!-- 新增：最新文章 -->
      <div class="recent-posts">
        <h2>最新文章</h2>
        {% include Feed.html %}
      </div>
    </div>
  {% else %}
    <!-- 原有的佈局 -->
    <h1>{{page.title}}</h1>
    {% include Content.html %}
    {% include Search.html %}
    {% include Feed.html %}
  {% endif %}
{% endif %}
```

然後添加對應的 CSS：

```css
.homepage {
  display: flex;
  flex-direction: column;
  gap: var(--space-xl);
}

.hero-section {
  text-align: center;
  padding: var(--space-xl) 0;
}

.featured-posts,
.recent-posts {
  background: var(--bg2);
  padding: var(--space-lg);
  border-radius: 8px;
}
```

### 5. 添加側邊欄

修改 `_layouts/Post.html` 的主體結構：

```html
<div class="container">
  {% include Nav.html %}

  <div class="main-layout">
    <!-- 主要內容區 -->
    <main class="content-area">
      <!-- 原有的內容 -->
    </main>

    <!-- 新增側邊欄 -->
    <aside class="sidebar">
      <div class="sidebar-section">
        <h3>關於我</h3>
        <p>簡短介紹...</p>
      </div>

      <div class="sidebar-section">
        <h3>最新文章</h3>
        <!-- 文章列表 -->
      </div>

      <div class="sidebar-section">
        <h3>標籤</h3>
        <!-- 標籤雲 -->
      </div>
    </aside>
  </div>

  {% include Footer.html %}
</div>
```

CSS 樣式：

```css
.main-layout {
  display: grid;
  grid-template-columns: 1fr 300px;  /* 主內容 + 300px 側邊欄 */
  gap: var(--space-lg);
  margin-top: var(--space-lg);
}

@media (max-width: 768px) {
  .main-layout {
    grid-template-columns: 1fr;  /* 手機上變成單欄 */
  }

  .sidebar {
    order: -1;  /* 移到上方 */
  }
}

.sidebar-section {
  background: var(--bg2);
  padding: var(--space-md);
  border-radius: 8px;
  margin-bottom: var(--space-md);
}
```

---

## 🔧 進階客製化

### 1. 添加深色模式切換按鈕

在 `assets/js/` 創建 `theme-toggle.js`：

```javascript
// Theme toggle functionality
const themeToggle = document.getElementById('theme-toggle');
const htmlElement = document.documentElement;

// Load saved theme
const savedTheme = localStorage.getItem('theme') || 'light';
htmlElement.setAttribute('data-theme', savedTheme);

// Toggle theme
themeToggle?.addEventListener('click', () => {
  const currentTheme = htmlElement.getAttribute('data-theme');
  const newTheme = currentTheme === 'light' ? 'dark' : 'light';

  htmlElement.setAttribute('data-theme', newTheme);
  localStorage.setItem('theme', newTheme);

  // Update button icon
  themeToggle.textContent = newTheme === 'light' ? '🌙' : '☀️';
});
```

在 `_layouts/Post.html` 底部引入：

```html
<script src="{{ site.baseurl }}/assets/js/theme-toggle.js"></script>
```

### 2. 添加閱讀時間估算

在 `_includes/Content.html` 中添加：

```liquid
{% if page.content %}
  {% assign words = page.content | number_of_words %}
  {% assign reading_time = words | divided_by: 200 %}
  <p class="reading-time">
    📖 閱讀時間：約 {{ reading_time }} 分鐘
  </p>
{% endif %}
```

### 3. 添加文章標籤功能

在文章 front matter 中添加：

```yaml
---
title: "文章標題"
tags: [Jekyll, Web Design, Tutorial]
---
```

在 `_includes/` 創建 `Tags.html`：

```liquid
{% if page.tags %}
<div class="tags">
  {% for tag in page.tags %}
    <a href="/tags/{{ tag | slugify }}" class="tag">
      #{{ tag }}
    </a>
  {% endfor %}
</div>
{% endif %}
```

CSS 樣式：

```css
.tags {
  display: flex;
  gap: var(--space-xs);
  flex-wrap: wrap;
  margin: var(--space-md) 0;
}

.tag {
  background: var(--bg2);
  padding: 0.25rem 0.75rem;
  border-radius: 12px;
  font-size: var(--scale-sm);
  text-decoration: none;
  transition: background 0.2s;
}

.tag:hover {
  background: var(--brand);
  color: white;
}
```

### 4. 自定義 404 頁面

編輯 `pages/404.md`：

```markdown
---
layout: Post
permalink: /404.html
title: "頁面不存在"
---

# 找不到這個頁面

你要找的頁面可能已經移動或刪除了。

[回到首頁](/) 或使用搜尋功能找找看？
```

---

## 部署流程

### 自動部署（推薦）

1. **修改檔案**（本地編輯）
2. **測試**（可選，本地預覽）：
   ```bash
   bundle exec jekyll serve
   # 訪問 http://localhost:4000
   ```
3. **提交變更**：
   ```bash
   git add .
   git commit -m "描述你的變更"
   git push origin main
   ```
4. **等待部署**：
   - GitHub Actions 會自動構建
   - 2-3 分鐘後網站更新
   - 查看狀態：https://github.com/yhchangcc/yhchangcc.github.io/actions

### 本地測試

```bash
# 1. 安裝依賴（首次）
bundle install

# 2. 啟動本地伺服器
bundle exec jekyll serve

# 3. 訪問 http://localhost:4000

# 4. 檔案會自動重新載入（除了 _config.yml）
```

---

## 🐛 常見問題

### Q1: 推送後網站沒有更新？

**解決方法**：
1. 檢查 GitHub Actions 狀態：
   - 訪問 https://github.com/yhchangcc/yhchangcc.github.io/actions
   - 查看是否有失敗的 workflow
2. 確認 GitHub Pages 設定：
   - Settings > Pages
   - Source 必須是 "GitHub Actions"
3. 清除瀏覽器快取（Cmd+Shift+R）

### Q2: CSS 樣式沒有生效？

**解決方法**：
1. 檢查 CSS 語法是否正確
2. 確認 `_config.yml` 中的 `baseurl` 設定
3. 檢查瀏覽器開發者工具的 Console 是否有錯誤
4. 清除快取並強制重新載入

### Q3: Wiki 連結 [[文章]] 無法工作？

**解決方法**：
1. 確認 `_config.yml` 中 `wiki_style_link` 已啟用：
   ```yaml
   preferences:
     wiki_style_link:
       enabled: true
   ```
2. 確認連結的文章檔案存在於 `_notes/` 目錄
3. 文章的 `title` 必須與連結文字完全一致

### Q4: 本地預覽正常，但部署後樣式錯誤？

**解決方法**：
- 使用 `{{ site.baseurl }}` 前綴所有資源路徑：
  ```liquid
  ❌ <link href="/assets/css/style.css">
  ✅ <link href="{{ site.baseurl }}/assets/css/style.css">
  ```

### Q5: 如何備份網站？

**方法 1 - Git 備份**（推薦）：
```bash
# 所有內容已在 Git 中，只需確保已推送
git push origin main
```

**方法 2 - 完整備份**：
```bash
# 壓縮整個目錄
tar -czf jekyll-garden-backup-$(date +%Y%m%d).tar.gz jekyll-garden/
```

---

## 進階學習資源

- **Jekyll 官方文件**：https://jekyllrb.com/docs/
- **Liquid 模板語言**：https://shopify.github.io/liquid/
- **GitHub Actions 文件**：https://docs.github.com/en/actions
- **Markdown 語法**：https://www.markdownguide.org/

---

## 🎯 快速參考

### 常用指令
```bash
# 本地預覽
bundle exec jekyll serve

# 推送更新
git add . && git commit -m "更新" && git push origin main

# 查看部署狀態
open https://github.com/yhchangcc/yhchangcc.github.io/actions
```

### 重要檔案
- `_config.yml` - 網站設定
- `assets/css/style.css` - 樣式設定
- `_layouts/Post.html` - 頁面版型
- `pages/index.md` - 首頁內容

### 需要修改時找這些檔案
- 改顏色 → `assets/css/style.css` (`:root`)
- 改選單 → `_config.yml` (`menu`)
- 改首頁 → `pages/index.md`
- 改導航 → `_includes/Nav.html`
- 改頁尾 → `_includes/Footer.html`

---

**最後更新**：2025-02-04
**版本**：1.0
